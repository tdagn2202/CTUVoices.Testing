# Test Reports

- `execution-results/`: kết quả thực thi theo cụm chức năng hoặc test cycle.
- `defects/`: danh sách lỗi phát hiện trong quá trình kiểm thử.
- `evidence/`: ảnh chụp và bằng chứng liên quan đến test case.

Không ghi `PASSED` hoặc `FAILED` trước khi test case được thực thi thực tế.
